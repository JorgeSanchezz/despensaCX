<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.despensacx">

    <!-- ✅ CORRECTO -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:name=".DespensaApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DespensaCX"
        tools:targetApi="31">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.DetalleListaActivity" android:exported="false" />
        <activity android:name=".ui.GestionTiendasActivity" android:exported="false" />
        <activity android:name=".ui.ListasArchivadasActivity" android:exported="false" />
        <activity android:name=".ui.EstadisticasActivity" android:exported="false" />
        <activity android:name=".ui.GestionCatalogoActivity" android:exported="false" />
        <activity android:name=".ui.ComparadorPreciosActivity" android:exported="false" />
        <activity android:name=".ui.GestionMembresiasActivity" android:exported="false" />

        <!-- FileProvider para compartir archivos generados si aplica -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.example.despensacx.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>